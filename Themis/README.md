# Themis

This is the Themis project, part of the Agentic monorepo.

## Overview

[Add project description here]

## Setup

[Add setup instructions here]

## Usage

Use the new `/evaluate` TrustSignal API in `src/themis/api.py` to evaluate query inputs, source documents, and compliance conflicts.

For local testing, install the project dependencies and use the FastAPI test client or run a local ASGI server.